### Installation

Using npm:

```bash
npm i --save mpan
```

In Node.js:

```node
import isValid from 'mpan';

isValid(value);
```

or

```node
const { isValid } = require('mpan');

isValid(value)
```
